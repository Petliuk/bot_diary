package com.example.bot_diary.pages_handler.comands.command_handler;


/*import org.telegram.telegrambots.bots.TelegramLongPollingBot;
import org.telegram.telegrambots.meta.api.methods.commands.SetMyCommands;
import org.telegram.telegrambots.meta.api.objects.commands.scope.BotCommandScopeDefault;
import org.telegram.telegrambots.meta.exceptions.TelegramApiException;

import javax.annotation.PostConstruct;
import java.util.ArrayList;
import java.util.List;

@Configuration
@Slf4j*/
public class CompletedTasksCommandHandler {

/*
    @Autowired
    private TelegramLongPollingBot bot;

    @PostConstruct
    public void init() {
        List<org.telegram.telegrambots.meta.api.objects.commands.BotCommand> listOfCommands = new ArrayList<>();
        listOfCommands.add(new org.telegram.telegrambots.meta.api.objects.commands.BotCommand("/start", "----"));
        listOfCommands.add(new org.telegram.telegrambots.meta.api.objects.commands.BotCommand("/alltasks", "----"));
        listOfCommands.add(new org.telegram.telegrambots.meta.api.objects.commands.BotCommand("/newtask", "----"));
        listOfCommands.add(new org.telegram.telegrambots.meta.api.objects.commands.BotCommand("/help", "info how to use this bot"));

        try {
            bot.execute(new SetMyCommands(listOfCommands, new BotCommandScopeDefault(), null));
        } catch (TelegramApiException e) {
            log.error("Error setting bot's command list: " + e.getMessage());
        }
    }
*/

}
